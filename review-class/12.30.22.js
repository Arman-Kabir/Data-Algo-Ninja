// prime number(check a number is prime or not)
// list out the prime number up to n(inefficient)
// Efficient solution - Sieve of Eratosthenes

function checkPrime(num){
    if(num == 0 || num ==1){
        console.log('Neither a prime or composite number');
        
    }
}

checkPrime(10);


// Mean,mode,median

// caser cipher